# Python
__pycache__/
*.pyc
*.pyo

# Segredos — NUNCA commitar
.streamlit/secrets.toml
.env

# Senha do painel da Ah Imobiliária. Os gravadores de tutorial precisam logar
# de verdade pra gravar o vídeo, mas a senha não mora mais dentro deles: vive
# aqui, e quem lê é o vendas/credenciais.py. Este repositório é PÚBLICO, e
# senha que entra no histórico do git não sai mais.
vendas/.credenciais-ah.json

# Chaves SSH da VM — NUNCA commitar
*.key
*.key.pub
ssh-key-*

# Claude Code (config local da máquina)
.claude/settings.local.json

# Contagem diária da cota do Groq (core/cota.py). É estado de execução:
# muda a cada mensagem e não serve pra ninguém no repositório.
.cota-groq.json

# SO
Thumbs.db
desktop.ini
